{

    // foamt
    "foam.openDailyNote.directory": "notes/daily",
    "foam.links.hover.enable": true,

    // paste image
    "pasteImage.showFilePathConfirmInputBox": true,
    "pasteImage.path": "${currentFileDir}/.media",
    "pasteImage.defaultName": "${currentFileNameWithoutExt}_YYYY-MM-DD_mm-ss",
    
}